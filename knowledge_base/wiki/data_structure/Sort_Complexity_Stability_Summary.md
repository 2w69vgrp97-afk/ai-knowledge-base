# Sort Complexity Stability Summary

## 核心概念

本页汇总常见排序算法的时间复杂度、空间复杂度和稳定性。

稳定排序指相等关键字排序后相对次序不变；不稳定排序指相等关键字排序后相对次序可能改变。

## 考试识别信号

- 比较多种排序算法。
- 判断最好、平均、最坏时间复杂度。
- 判断空间复杂度或稳定性。
- 根据数据特点选择排序方法。

## 核心公式 / 代码模板

| 排序 | 最好 | 平均 | 最坏 | 比较次数 | 空间 | 稳定性 |
| --- | --- | --- | --- | --- | --- | --- |
| 直接插入 | O(n) | O(n^2) | O(n^2) | O(n) 到 O(n^2) | O(1) | 稳定 |
| 折半插入 | 按教材口径；移动少但仍有折半查找比较 | O(n^2) | O(n^2) | O(n log n) | O(1) | 稳定 |
| 希尔 | 与增量有关 | 与增量有关 | 与增量有关 | 与增量有关 | O(1) | 不稳定 |
| 冒泡 | O(n) | O(n^2) | O(n^2) | O(n) 到 O(n^2) | O(1) | 稳定 |
| 简单选择 | O(n^2) | O(n^2) | O(n^2) | O(n^2) | O(1) | 不稳定 |
| 快速 | O(n log n) | O(n log n) | O(n^2) | O(n log n) 到 O(n^2) | O(log n) | 不稳定 |
| 堆 | O(n log n) | O(n log n) | O(n log n) | O(n log n) | O(1) | 不稳定 |
| 归并 | O(n log n) | O(n log n) | O(n log n) | O(n log n) | O(n) | 稳定 |
| 基数 | O(d(n + r)) | O(d(n + r)) | O(d(n + r)) | 非比较排序 | O(r) 或 O(n + r) | 稳定 |

折半插入排序只减少比较次数，不减少移动次数。平均和最坏整体时间复杂度仍为 `O(n^2)`，比较次数通常约为 `O(n log n)`。最好情况按课程教材口径处理。

## 手写步骤 / 计算步骤

1. 先判断题目问时间、空间还是稳定性。
2. 区分最好、平均、最坏情况。
3. 判断是否会改变相等关键字相对次序。
4. 根据初始有序程度、数据规模、空间限制选择算法。

## 边界条件

初始基本有序时，直接插入和带交换标志的冒泡表现较好。

要求最坏情况稳定 `O(n log n)` 时，可考虑归并排序。空间受限时，堆排序常作为选择之一。

## 常见错误

- 把选择排序误认为稳定。
- 把快速排序最坏复杂度写成 `O(n log n)`。
- 认为折半插入排序整体是 `O(n log n)`。
- 忽略归并排序需要 `O(n)` 辅助空间。
- 把希尔排序写成稳定。

## 典型题型

- 填排序复杂度表。
- 判断排序稳定性。
- 根据场景选择排序算法。

## 复杂度分析

见本页汇总表。考试时要区分“比较次数”“移动次数”和“整体时间复杂度”。

## 与其他知识的联系

本页汇总 [[Sort_Basic]]、[[Binary_Insertion_Sort]]、[[Shell_Sort_Basic]]、[[Heap_Sort_Basic]]、[[Radix_Sort_Basic]] 和 [[QuickSort_Partition]]。

## 相关链接

- [[Sort_Basic]]
- [[Binary_Insertion_Sort]]
- [[Shell_Sort_Basic]]
- [[Heap_Sort_Basic]]
- [[Radix_Sort_Basic]]
- [[QuickSort_Partition]]

## 待追问问题

- 当前教材对折半插入排序最好情况如何表述？
- 归并排序递归版和非递归版空间复杂度是否不同？

## 来源

- knowledge_base/raw/data_structure/sort_complexity_stability_summary.txt
