# AI 知识库维护规则

- `knowledge_base/raw/` 存放原始资料，不直接修改。
- `knowledge_base/wiki/` 存放结构化知识页。
- `knowledge_base/outputs/` 存放临时问答结果。
- 生成 `wiki/` 内容时必须保留来源说明。
- 遇到不确定内容要标注“待核实”。
- 优先生成清晰标题、概念解释、关联链接和待追问问题。
- 不要凭空编造 `raw/` 中没有的事实。
